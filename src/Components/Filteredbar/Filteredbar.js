import React from "react";
import Pagination from "../Pagination/Pagination";

const Filteredbar = () => {
  return (
    <div>
      Filteredbar
      <Pagination />
    </div>
  );
};

export default Filteredbar;
